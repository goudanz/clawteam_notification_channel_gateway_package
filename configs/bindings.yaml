# 通用绑定规则：channel -> app -> chat -> target(team/agent)
channels:
  feishu:
    apps:
      # dev-bot
      cli_a94ac1f74c785cb2:
        chats:
          default:
            team: gateway-dev
            agent: dev
            mode: inbox
            timeout_sec: 90

      # market-bot
      cli_a94e557be5399bc3:
        chats:
          default:
            team: gateway-market
            agent: market
            mode: inbox
            timeout_sec: 90
